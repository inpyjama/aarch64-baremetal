# Setup core frequency
core_freq_min=500

# Setup default core frequency
core_freq=500

# Enable UART
enable_uart=1

# Disable pull downs
gpio=22-27=np

# Enable jtag pins (i.e. GPIO22-GPIO27)
enable_jtag_gpio=1

# Boot in EL3
armstub=armstub8.bin
